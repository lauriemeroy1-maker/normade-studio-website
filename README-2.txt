Place your logo file here as:
  normade-logo.png

I wasn't able to download it automatically from the ibb.co link you
shared (the host's robots.txt blocks automated fetching), so please
export/save the PNG and drop it in this folder with that exact name.
The site already references it in the nav bar and footer (inverted
to white on the black footer via CSS).
