Drop your Malinton Trial Version files here, named:
  MalintonTrialVersion-Bold.otf
  MalintonTrialVersion-Bold.ttf
  (a .woff2 too, if you have/convert one — smaller & loads faster)

style.css already points to these filenames via @font-face, so all
headings will pick the font up automatically once it's here.

Important — license: Malinton Trial Version (Zarma Type) is free
for PERSONAL USE ONLY. For a client-facing commercial site you'll
need the paid commercial license before launch:
https://zarmatype.com/font/malinton-playful-display-font/
Until then, headings fall back to Arial Black so the site still
looks intentional.
